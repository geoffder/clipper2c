# clipper2c

C bindings for the Clipper2 polygon clipping and offsetting library

Run `git submodule update --init --recursive` after cloning to bring the correct version of Clipper2 into the `vendor` sub-directory.
